# Base URL for backend APIs (no trailing slash)
VITE_API_BASE_URL=https://api.example.com

# Toggle real API mode (true to call backend, false to use mocks)
VITE_USE_REAL_API=false

# If backend uses cookie-based auth and requires credentials on CORS
# VITE_API_WITH_CREDENTIALS=true
