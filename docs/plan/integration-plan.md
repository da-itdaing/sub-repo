# 통합 계획서 (Integration Plan)

## 📋 프로젝트 개요

**프로젝트명**: Itdaing (잇다잉) - 팝업스토어 추천 플랫폼  
**통합 범위**: 프론트엔드(React) + 백엔드(Spring Boot) 통합  
**통합 목표**: 프론트엔드와 백엔드 간 API 연동, 인증 시스템 통합, 데이터 흐름 최적화

---

## 🏗️ 아키텍처 개요

### 시스템 구조

```
┌─────────────────┐
│   Frontend       │
│   (React)        │
│   Port: 3000     │
└────────┬─────────┘
         │ HTTP/HTTPS
         │ /api/* → Proxy
         ▼
┌─────────────────┐
│   Backend        │
│   (Spring Boot)  │
│   Port: 8080     │
└────────┬─────────┘
         │ JDBC
         ▼
┌─────────────────┐
│   PostgreSQL    │
│   (AWS RDS)      │
└─────────────────┘
```

### 프로젝트 구조

```
itdaing/
├── itdaing-web/          # 프론트엔드 (React + TypeScript + Vite)
│   ├── src/
│   │   ├── services/     # API 서비스 레이어
│   │   ├── hooks/        # React Hooks (데이터 페칭)
│   │   ├── contexts/     # Context API (인증, 사용자)
│   │   └── components/   # UI 컴포넌트
│   └── vite.config.ts    # Vite 설정 (프록시 포함)
│
└── src/                  # 백엔드 (Spring Boot)
    └── main/java/
        └── com/da/itdaing/
            ├── domain/   # 도메인별 모듈
            ├── global/   # 전역 설정
            └── config/   # 설정 클래스
```

---

## ✅ 완료된 통합 작업

### 1. 개발 환경 통합

#### 1.1 프록시 설정
- ✅ Vite 개발 서버 프록시 설정 완료
  - 경로: `/api` → `http://localhost:8080`
  - 설정 파일: `itdaing-web/vite.config.ts`
  - 개발 모드에서 CORS 문제 해결

#### 1.2 환경 변수 관리
- ✅ 프론트엔드: `.env.example` 파일 제공
- ✅ 백엔드: `prod.env` 파일로 프로덕션 환경 변수 관리
- ✅ 환경별 설정 분리 (local, test, prod)

### 2. 인증 시스템 통합

#### 2.1 JWT 인증 플로우
- ✅ 백엔드: JWT 토큰 생성/검증 (`JwtTokenProvider`)
- ✅ 백엔드: Spring Security 필터 (`JwtAuthFilter`)
- ✅ 프론트엔드: Axios 인터셉터로 토큰 자동 추가
- ✅ 프론트엔드: 토큰 저장소 (localStorage)
- ✅ 프론트엔드: 인증 상태 관리 (`AuthContext`)

#### 2.2 인증 API 연동
- ✅ 로그인 (`POST /api/auth/login`)
  - 프론트엔드: `authService.login()`
  - 백엔드: `AuthController.login()`
- ✅ 소비자 회원가입 (`POST /api/auth/signup/consumer`)
  - 프론트엔드: `authService.signupConsumer()`
  - 백엔드: `AuthController.signupConsumer()`
- ✅ 판매자 회원가입 (`POST /api/auth/signup/seller`)
  - 프론트엔드: `authService.signupSeller()`
  - 백엔드: `AuthController.signupSeller()`
- ✅ 내 프로필 조회 (`GET /api/users/me`)
  - 프론트엔드: `authService.getMyProfile()`
  - 백엔드: `UserController.getMe()`
- ✅ 토큰 재발급 (`POST /api/auth/refresh`)
  - 프론트엔드: `authService.refreshToken()`
  - 백엔드: `AuthController.refresh()`
- ✅ 로그아웃 (`POST /api/auth/logout`)
  - 프론트엔드: `authService.logout()`
  - 백엔드: `AuthController.logout()`

### 3. 마스터 데이터 API 연동

#### 3.1 마스터 데이터 조회
- ✅ 카테고리 목록 (`GET /api/master/categories`)
  - 프론트엔드: `masterService.getCategories()`
  - 백엔드: `MasterQueryController.getCategories()`
- ✅ 스타일 목록 (`GET /api/master/styles`)
  - 프론트엔드: `masterService.getStyles()`
  - 백엔드: `MasterQueryController.getStyles()`
- ✅ 지역 목록 (`GET /api/master/regions`)
  - 프론트엔드: `masterService.getRegions()`
  - 백엔드: `MasterQueryController.getRegions()`
- ✅ 편의시설 목록 (`GET /api/master/features`)
  - 프론트엔드: `masterService.getFeatures()`
  - 백엔드: `MasterQueryController.getFeatures()`

### 4. 팝업(Popup) API 연동

#### 4.1 팝업 조회
- ✅ 팝업 목록 조회 (`GET /api/popups`)
  - 프론트엔드: `popupService.getPopups()`
  - Hook: `usePopups()`
  - 백엔드: `PopupQueryController.getPopups()`
- ✅ 팝업 상세 조회 (`GET /api/popups/{id}`)
  - 프론트엔드: `popupService.getPopupById()`
  - Hook: `usePopupById()`
  - 백엔드: `PopupQueryController.getPopupById()`

#### 4.2 팝업 관리 (판매자용)
- ✅ 팝업 등록 (`POST /api/popups`)
  - 프론트엔드: `popupService.createPopup()`
  - UI: `PopupForm` 컴포넌트 연동 완료
  - 백엔드: `PopupCommandController.createPopup()`
- ✅ 팝업 수정 (`PUT /api/popups/{id}`)
  - 프론트엔드: `popupService.updatePopup()`
  - UI: `PopupForm` 컴포넌트 수정 모드 지원
  - 백엔드: `PopupCommandController.updatePopup()`
- ✅ 팝업 삭제 (`DELETE /api/popups/{id}`)
  - 프론트엔드: `popupService.deletePopup()`
  - UI: `PopupManagementPage` 연동 완료
  - 백엔드: `PopupCommandController.deletePopup()`
- ✅ 내 팝업 목록 조회 (`GET /api/sellers/me/popups`)
  - 프론트엔드: `sellerService.getMyPopups()`
  - UI: `PopupManagementPage` 연동 완료
  - 백엔드: `SellerController.getMyPopups()`

### 5. 판매자(Seller) API 연동

#### 5.1 판매자 조회
- ✅ 판매자 목록 조회 (`GET /api/sellers`)
  - 프론트엔드: `sellerService.getSellers()`
  - Hook: `useSellers()`
  - 백엔드: `SellerController.getSellers()`
- ✅ 판매자 상세 조회 (`GET /api/sellers/{id}`)
  - 프론트엔드: `sellerService.getSellerById()`
  - Hook: `useSellerById()`
  - 백엔드: `SellerController.getSellerById()`

#### 5.2 판매자 프로필 관리
- ✅ 내 프로필 조회 (`GET /api/sellers/me/profile`)
  - 프론트엔드: `sellerService.getMyProfile()`
  - 백엔드: `SellerController.getMyProfile()`
- ✅ 내 프로필 수정 (`PUT /api/sellers/me/profile`)
  - 프론트엔드: `sellerService.updateProfile()`
  - UI: `SellerProfileForm` 컴포넌트 연동 완료
  - 백엔드: `SellerController.updateProfile()`

### 6. 리뷰(Review) API 연동

#### 6.1 리뷰 조회
- ✅ 팝업별 리뷰 목록 (`GET /api/popups/{id}/reviews`)
  - 프론트엔드: `reviewService.getReviewsByPopupId()`
  - Hook: `useReviewsByPopupId()`
  - 백엔드: `PopupQueryController.getReviewsByPopupId()`
- ✅ 전체 리뷰 목록 (`GET /api/popups/reviews`)
  - 프론트엔드: `reviewService.getReviews()`
  - 백엔드: `PopupQueryController.getReviews()`

#### 6.2 리뷰 관리 (소비자용)
- ✅ 리뷰 작성 (`POST /api/popups/{popupId}/reviews`)
  - 프론트엔드: `reviewService.createReview()`
  - UI: `ReviewWritePage` 컴포넌트 연동 완료
  - 백엔드: `ReviewCommandController.createReview()`
- ✅ 리뷰 수정 (`PUT /api/reviews/{id}`)
  - 프론트엔드: `reviewService.updateReview()`
  - UI: `ReviewWritePage` 수정 모드 지원
  - 백엔드: `ReviewCommandController.updateReview()`
- ✅ 리뷰 삭제 (`DELETE /api/reviews/{id}`)
  - 프론트엔드: `reviewService.deleteReview()`
  - UI: `PopupDetailPage` 연동 완료
  - 백엔드: `ReviewCommandController.deleteReview()`

### 7. 메시지(Message) API 연동

#### 7.1 메시지 스레드 관리
- ✅ 스레드 생성 및 첫 메시지 전송 (`POST /api/inquiries`)
  - 프론트엔드: 준비 중
  - 백엔드: `MessageController.createThread()`
- ✅ 스레드 목록 조회 (`GET /api/inquiries`)
  - 프론트엔드: 준비 중
  - 백엔드: `MessageController.getThreads()`
- ✅ 스레드 상세 조회 (`GET /api/inquiries/{threadId}`)
  - 프론트엔드: 준비 중
  - 백엔드: `MessageController.getThread()`
- ✅ 답장 전송 (`POST /api/inquiries/{threadId}/reply`)
  - 프론트엔드: 준비 중
  - 백엔드: `MessageController.reply()`
- ✅ 메시지 삭제 (`DELETE /api/inquiries/messages/{messageId}`)
  - 프론트엔드: 준비 중
  - 백엔드: `MessageController.deleteMessage()`

### 8. 지역(Zone) API 연동

#### 8.1 존(Zone) 조회
- ✅ 존 목록 조회 (`GET /api/zones`)
  - 프론트엔드: 준비 중
  - 백엔드: `ZoneQueryController.getZones()`

#### 8.2 존/셀 관리 (관리자/판매자용) ✅ 완료
- ✅ 구역 생성 (`POST /api/geo/areas`) - 관리자 전용
  - 프론트엔드: `geoService.createArea()` 완료
  - UI: `AreaFormDialog` 컴포넌트 연동 완료
  - 백엔드: `GeoAreaController.createArea()`
- ✅ 구역 목록 조회 (`GET /api/geo/areas`) - 관리자/판매자 읽기 가능
  - 프론트엔드: `geoService.getAreas()` 완료
  - 백엔드: `GeoAreaController.getAreas()`
- ✅ 구역 상세 조회 (`GET /api/geo/areas/{id}`) - 관리자/판매자 읽기 가능
  - 프론트엔드: `geoService.getAreaById()` 완료
  - 백엔드: `GeoAreaController.getArea()`
- ✅ 구역 수정 (`PUT /api/geo/areas/{id}`) - 관리자 전용
  - 프론트엔드: `geoService.updateArea()` 완료
  - UI: `AreaFormDialog` 컴포넌트 수정 모드 지원
  - 백엔드: `GeoAreaController.updateArea()`
- ✅ 구역 삭제 (`DELETE /api/geo/areas/{id}`) - 관리자 전용
  - 프론트엔드: `geoService.deleteArea()` 완료
  - UI: `ZoneManagement.tsx` 연동 완료
  - 백엔드: `GeoAreaController.deleteArea()`
- ✅ 존 생성 (`POST /api/geo/zones`) - 관리자 전용 (판매자 ID 지정)
  - 프론트엔드: `geoService.createZone()` 완료
  - 백엔드: `GeoZoneController.createZone()` (권한 변경: SELLER → ADMIN)
- ✅ 내가 할당받은 존 목록 (`GET /api/geo/zones/me`) - 판매자 읽기 전용
  - 프론트엔드: `geoService.getMyZones()` 완료
  - 백엔드: `GeoZoneController.getMyZones()`
- ✅ 구역별 존 목록 (`GET /api/geo/zones`) - 관리자 전용
  - 프론트엔드: `geoService.getZones()` 완료
  - 백엔드: `GeoZoneController.getZones()`
- ✅ 존 상태 변경 (`PATCH /api/geo/zones/{zoneId}/status`) - 관리자 전용
  - 프론트엔드: `geoService.updateZoneStatus()` 완료
  - UI: `ZoneManagement.tsx` 연동 완료
  - 백엔드: `GeoZoneController.updateZoneStatus()`
- ✅ 셀 생성 (`POST /api/geo/cells`) - 관리자 전용 (판매자 ID 지정)
  - 프론트엔드: `geoService.createCell()` 완료
  - UI: `CellFormDialog` 컴포넌트 연동 완료
  - 백엔드: `GeoCellController.createCell()`
- ✅ 셀 수정 (`PUT /api/geo/cells/{id}`) - 관리자 전용
  - 프론트엔드: `geoService.updateCell()` 완료
  - UI: `CellFormDialog` 컴포넌트 수정 모드 지원
  - 백엔드: `GeoCellController.updateCell()`
- ✅ 셀 삭제 (`DELETE /api/geo/cells/{id}`) - 관리자 전용
  - 프론트엔드: `geoService.deleteCell()` 완료
  - UI: `ZoneManagement.tsx` 연동 완료
  - 백엔드: `GeoCellController.deleteCell()`
- ✅ 셀 목록 조회 (`GET /api/geo/cells`) - 관리자/판매자 읽기 가능
  - 프론트엔드: `geoService.getCells()` 완료
  - UI: `PopupForm`, `NearbyExplorePage` 연동 완료
  - 백엔드: `GeoCellController.listCells()`
- ✅ 셀 상세 조회 (`GET /api/geo/cells/{id}`) - 관리자/판매자 읽기 가능
  - 프론트엔드: `geoService.getCellById()` 완료
  - UI: `PopupDetailPage` 연동 완료
  - 백엔드: `GeoCellController.getCell()`

### 9. 파일 업로드 API 연동 ✅ 완료

#### 9.1 이미지 업로드
- ✅ 이미지 업로드 (`POST /api/uploads/images`)
  - 프론트엔드: `uploadService.uploadImages()` 구현 완료 ✅
  - 백엔드: `UploadController.uploadImage()`
  - 스토리지: AWS S3 연동 완료
  - UI 통합: PopupForm, ReviewWritePage, SellerProfileForm에 ImageUploader 통합 완료 ✅

### 10. 에러 처리 통합

#### 10.1 에러 핸들링
- ✅ 백엔드: 전역 예외 처리 (`GlobalExceptionHandler`)
  - 응답 형식: `{success: false, error: {...}}`
- ✅ 프론트엔드: 에러 바운더리 (`ErrorBoundary`)
- ✅ 프론트엔드: 네트워크 에러 처리 개선
- ✅ 프론트엔드: 재시도 로직 추가 (`retryRequest`)

#### 10.2 인증 에러 처리
- ✅ 401 에러 처리 (토큰 만료/무효)
  - 프론트엔드: 자동 로그아웃 및 로그인 페이지 리다이렉트
- ✅ 403 에러 처리 (권한 없음)
  - 프론트엔드: 권한 없음 메시지 표시

---

## 🚧 진행 중인 통합 작업

### 1. UI 목업 구현 ✅ 완료
- **상태**: 참고 이미지 기반 관리자/판매자 대시보드 UI 목업 구현 완료
- **완료된 작업**:
  - ✅ 공통 컴포넌트: StatCard, ViewsChart, DataTable
  - ✅ 관리자 대시보드: 승인 현황 카드, 구역 관리 지도, 검수 관리 테이블
  - ✅ 판매자 대시보드: 승인 현황 카드, 팝업 현황 카드, 조회수 차트, 팝업 관리 테이블
  - ✅ 헤더/사이드바: 관리자/판매자 레이아웃 업데이트
- **남은 작업**:
  - 실제 API 데이터 연동 (현재는 Mock 데이터 사용)
  - 카테고리/구역 정보 표시 개선

### 2. 메시지 시스템 UI 연동 ✅ 완료
- **상태**: 백엔드 API 및 프론트엔드 UI 연동 완료
- **완료된 작업**:
  - ✅ 메시지 타입 정의 (`types/message.ts`)
  - ✅ 메시지 서비스 레이어 (`services/messageService.ts`)
  - ✅ React Hooks 구현 (`useMessageThreads`, `useMessageThread`)
  - ✅ 공통 컴포넌트: MessageThreadList, MessageThreadDetail, MessageComposer
  - ✅ 판매자 메시지 페이지 UI (`/seller/messages`)
  - ✅ 관리자 메시지 페이지 UI (`/admin/messages`)
  - ✅ 스레드 목록, 상세, 답장 기능 완료

### 2. 지역(Zone) 관리 UI 연동 ✅ 완료
- **상태**: 백엔드 API 및 프론트엔드 UI 연동 완료 ✅
- **완료된 작업**:
  - ✅ 프론트엔드 서비스 레이어 완료 (`geoService`)
  - ✅ 승인 관리 UI 연동 완료 (`Approvals.tsx`)
  - ✅ 관리자: 구역(Area) 생성/수정/삭제 UI 완료 (`ZoneManagement.tsx`, `AreaFormDialog`)
  - ✅ 관리자: 셀(Cell) 생성/수정/삭제 UI 완료 (`ZoneManagement.tsx`, `CellFormDialog`)
  - ✅ Kakao Map API 통합 완료 (`KakaoMapAreaEditor`, `KakaoMapCellSelector`)
  - ✅ 관리자 워크플로우 개선: Area 생성 후 자동 선택 및 Cells 탭 전환
  - ✅ 관리자 워크플로우 개선: Cell 생성 시 선택된 Area 컨텍스트 유지
  - ✅ 판매자: Cell 선택 UI 완료 (`PopupForm`에 `KakaoMapCellSelector` 통합)
  - ✅ 소비자: 팝업 위치 지도 표시 완료 (`PopupDetailPage` 지도 탭, `NearbyExplorePage`)

### 3. 파일 업로드 UI 연동 ✅ 완료
- **상태**: 백엔드 API 및 프론트엔드 UI 연동 완료
- **완료된 작업**:
  - ✅ 이미지 업로드 서비스 (`services/uploadService.ts`)
  - ✅ ImageUploader 공통 컴포넌트 구현 (드래그 앤 드롭, 미리보기, 파일 검증)
  - ✅ PopupForm에 썸네일/갤러리 이미지 업로드 통합
  - ✅ ReviewWritePage에 리뷰 이미지 업로드 통합
  - ✅ SellerProfileForm에 프로필 이미지 업로드 통합
  - ✅ 기존 이미지 미리보기 및 삭제 기능

### 4. 승인 관리 UI 연동
- **상태**: 백엔드 API 및 프론트엔드 UI 연동 완료 ✅
- **완료된 작업**:
  - ✅ 승인 대기 목록 조회 UI (`Approvals.tsx`)
  - ✅ 승인/거부 처리 UI (`Approvals.tsx`)
  - ✅ 프론트엔드 서비스 레이어 (`approvalService`)

---

## 📝 앞으로 해야 할 통합 작업

### 1. 우선순위: 높음

#### 1.1 Kakao Map API 통합
- **목표**: 관리자/판매자 페이지에서 지도 기능 구현
- **필요 작업**:
  - `react-kakao-maps-sdk` 패키지 설치
  - Kakao Map API 키 환경 변수 설정
  - 관리자: Zone/Cell 그리기 기능 (DrawingManager)
  - 판매자: Zone/Cell 선택 기능
  - 소비자: 팝업 위치 표시
- **예상 소요 시간**: 2-3일

#### 1.2 관리자용 API UI 연동
- **목표**: 관리자 대시보드 기능 완성
- **필요 작업**:
  - 셀(Cell) 생성/수정/삭제 UI
  - 구역(Area) 수정/삭제 UI
  - 승인 관리 UI
- **예상 소요 시간**: 2-3일

#### 1.3 검색 및 필터링 API 연동
- **목표**: 팝업 검색 기능 구현
- **필요 작업**:
  - 백엔드: 검색 API 구현 (QueryDSL 활용)
  - 프론트엔드: 검색 UI 구현
  - 필터링 UI 구현 (지역, 카테고리, 날짜 범위)
- **예상 소요 시간**: 3-4일

### 2. 우선순위: 중간

#### 2.1 추천 시스템 API 연동
- **목표**: 소비자 맞춤 추천 기능 구현
- **필요 작업**:
  - 백엔드: 추천 알고리즘 구현
  - 프론트엔드: 추천 팝업 표시 UI
- **예상 소요 시간**: 3-5일

#### 2.2 페이지네이션 및 무한 스크롤
- **목표**: 대량 데이터 효율적 표시
- **필요 작업**:
  - 백엔드: 페이지네이션 응답 형식 통일
  - 프론트엔드: 무한 스크롤 또는 페이지네이션 UI
- **예상 소요 시간**: 1-2일

#### 2.3 API 응답 캐싱 전략
- **목표**: API 호출 최적화
- **필요 작업**:
  - React Query 또는 SWR 도입 검토
  - 캐싱 전략 수립
  - 캐시 무효화 로직 구현
- **예상 소요 시간**: 2-3일

### 3. 우선순위: 낮음

#### 3.1 실시간 알림 시스템
- **목표**: 메시지/승인 알림 실시간 표시
- **필요 작업**:
  - WebSocket 또는 Server-Sent Events (SSE) 통합
  - 프론트엔드: 알림 UI 구현
- **예상 소요 시간**: 5-7일

#### 3.2 통계 및 분석 대시보드
- **목표**: 판매자/관리자 대시보드 통계 표시
- **필요 작업**:
  - 백엔드: 통계 API 구현
  - 프론트엔드: 차트 라이브러리 통합 (Recharts)
- **예상 소요 시간**: 3-4일

---

## 🔄 데이터 흐름 (Data Flow)

### 1. 인증 플로우

```
┌──────────┐                    ┌──────────┐                    ┌──────────┐
│ Frontend │                    │ Backend  │                    │ Database │
└────┬─────┘                    └────┬─────┘                    └────┬─────┘
     │                               │                               │
     │ 1. POST /api/auth/login       │                               │
     │    {loginId, password}        │                               │
     ├──────────────────────────────>│                               │
     │                               │ 2. 사용자 조회                │
     │                               ├──────────────────────────────>│
     │                               │<──────────────────────────────┤
     │                               │ 3. 비밀번호 검증              │
     │                               │ 4. JWT 토큰 생성              │
     │                               │                               │
     │ 5. {accessToken, refreshToken}│                               │
     │<──────────────────────────────┤                               │
     │                               │                               │
     │ 6. 토큰 저장 (localStorage)    │                               │
     │                               │                               │
```

### 2. 팝업 조회 플로우

```
┌──────────┐                    ┌──────────┐                    ┌──────────┐
│ Frontend │                    │ Backend  │                    │ Database │
└────┬─────┘                    └────┬─────┘                    └────┬─────┘
     │                               │                               │
     │ 1. GET /api/popups            │                               │
     │    Authorization: Bearer {token}│                             │
     ├──────────────────────────────>│                               │
     │                               │ 2. 토큰 검증                  │
     │                               │ 3. 팝업 목록 조회             │
     │                               ├──────────────────────────────>│
     │                               │<──────────────────────────────┤
     │                               │ 4. DTO 변환                   │
     │                               │                               │
     │ 5. {success: true, data: [...]}│                            │
     │<──────────────────────────────┤                               │
     │                               │                               │
     │ 6. 상태 업데이트 (React State) │                               │
     │                               │                               │
```

### 3. 팝업 등록 플로우

```
┌──────────┐                    ┌──────────┐                    ┌──────────┐
│ Frontend │                    │ Backend  │                    │ Database │
└────┬─────┘                    └────┬─────┘                    └────┬─────┘
     │                               │                               │
     │ 1. POST /api/popups            │                               │
     │    FormData (이미지 포함)      │                               │
     │    Authorization: Bearer {token}│                             │
     ├──────────────────────────────>│                               │
     │                               │ 2. 토큰 검증                  │
     │                               │ 3. 판매자 권한 확인            │
     │                               │ 4. 이미지 업로드 (S3)         │
     │                               ├──────────────────────────────>│
     │                               │<──────────────────────────────┤
     │                               │ 5. 팝업 엔티티 생성           │
     │                               ├──────────────────────────────>│
     │                               │<──────────────────────────────┤
     │                               │                               │
     │ 6. {success: true, data: {...}}│                              │
     │<──────────────────────────────┤                               │
     │                               │                               │
```

---

## 🧪 통합 테스트 전략

### 1. API 통합 테스트

#### 1.1 백엔드 API 테스트
- **도구**: Spring Boot Test + MockMvc
- **범위**: 
  - 인증 API 테스트
  - CRUD API 테스트
  - 권한 검증 테스트
- **위치**: `src/test/java/com/da/itdaing/domain/*/api/*ControllerTest.java`

#### 1.2 프론트엔드 API 통합 테스트
- **도구**: Vitest + MSW (Mock Service Worker)
- **범위**:
  - 서비스 레이어 테스트
  - Hook 테스트
  - 에러 처리 테스트
- **위치**: `itdaing-web/src/**/*.test.ts`

### 2. E2E 통합 테스트

#### 2.1 사용자 시나리오 테스트
- **도구**: Playwright 또는 Cypress
- **시나리오**:
  - 소비자: 로그인 → 팝업 조회 → 리뷰 작성
  - 판매자: 로그인 → 팝업 등록 → 팝업 수정
  - 관리자: 로그인 → 존 관리 → 승인 처리

### 3. 수동 통합 테스트 체크리스트

#### 3.1 인증 플로우
- [ ] 로그인 성공
- [ ] 로그인 실패 (잘못된 자격증명)
- [ ] 토큰 만료 시 자동 로그아웃
- [ ] 토큰 재발급
- [ ] 로그아웃

#### 3.2 팝업 관리 플로우
- [ ] 팝업 목록 조회
- [ ] 팝업 상세 조회
- [ ] 팝업 등록 (이미지 포함)
- [ ] 팝업 수정
- [ ] 팝업 삭제

#### 3.3 리뷰 관리 플로우
- [ ] 리뷰 목록 조회
- [ ] 리뷰 작성
- [ ] 리뷰 수정
- [ ] 리뷰 삭제

---

## 🚀 배포 통합

### 1. 개발 환경

#### 1.1 로컬 개발 환경
- **프론트엔드**: Vite 개발 서버 (포트 3000)
- **백엔드**: Spring Boot (포트 8080)
- **프록시**: Vite 프록시 설정 (`/api` → `http://localhost:8080`)

#### 1.2 실행 방법
```bash
# 백엔드 실행
cd ~/itdaing
source prod.env
./gradlew bootRun

# 프론트엔드 실행 (별도 터미널)
cd ~/itdaing/itdaing-web
npm run dev
```

### 2. 프로덕션 환경

#### 2.1 배포 구조
- **프론트엔드**: 정적 파일 (Nginx 또는 S3 + CloudFront)
- **백엔드**: Spring Boot JAR (systemd 서비스)
- **데이터베이스**: AWS RDS PostgreSQL

#### 2.2 배포 프로세스
1. 백엔드 빌드 및 배포
   ```bash
   ./gradlew build -x test
   # JAR 파일을 EC2에 배포
   # systemd 서비스 재시작
   ```

2. 프론트엔드 빌드 및 배포
   ```bash
   cd itdaing-web
   npm run build
   # dist/ 폴더를 Nginx 또는 S3에 배포
   ```

#### 2.3 환경 변수 설정
- **백엔드**: `prod.env` 파일로 관리
- **프론트엔드**: 빌드 시 환경 변수 주입
- **민감 정보**: AWS Secrets Manager 또는 환경 변수로 관리

---

## 🔐 보안 통합

### 1. 인증 및 권한

#### 1.1 JWT 토큰 관리
- **Access Token**: 짧은 만료 시간 (예: 1시간)
- **Refresh Token**: 긴 만료 시간 (예: 7일)
- **토큰 저장**: localStorage (프론트엔드)
- **토큰 전송**: Authorization 헤더

#### 1.2 CORS 설정
- **백엔드**: `SecurityConfig`에서 CORS 설정
- **프론트엔드**: 개발 환경에서 프록시 사용

### 2. 데이터 보안

#### 2.1 입력 검증
- **백엔드**: Bean Validation (`@Valid`, `@NotNull` 등)
- **프론트엔드**: React Hook Form + Yup 검증

#### 2.2 XSS 방지
- **백엔드**: 입력 데이터 sanitization
- **프론트엔드**: React의 기본 XSS 방지 기능 활용

---

## 📊 API 응답 형식 통일

### 1. 성공 응답

```json
{
  "success": true,
  "data": {
    // 응답 데이터
  }
}
```

### 2. 에러 응답

```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": "에러 메시지",
    "details": {}
  }
}
```

### 3. 페이지네이션 응답

```json
{
  "success": true,
  "data": {
    "content": [],
    "page": 0,
    "size": 20,
    "totalElements": 100,
    "totalPages": 5
  }
}
```

---

## 🐛 알려진 통합 이슈

### 1. 해결된 이슈
- ✅ CORS 문제: Vite 프록시로 해결
- ✅ API 응답 형식 불일치: 통일된 형식으로 수정 완료
- ✅ 인증 토큰 자동 추가: Axios 인터셉터로 해결
- ✅ 데이터베이스 연결: AWS RDS PostgreSQL 연결 확인 완료 (2025-01-13)
- ✅ 초기 테스트 데이터: 테스트 계정 생성 완료 (2025-01-13)
- ✅ 비밀번호 해시: BCrypt 해시 업데이트 완료 (2025-01-13)
- ✅ 프론트엔드 로그인 폼: form 태그 및 검증 추가로 해결 (2025-01-13)
- ✅ 로그인 API 문서: loginId 기반으로 수정 완료 (2025-01-13)
- ✅ 카카오맵 API 키: JavaScript 키 업데이트 완료 (2025-01-27)
- ✅ 더미 데이터: 광주광역시 플리마켓 플랫폼 더미 데이터 스크립트 작성 완료 (2025-01-27)

### 2. 진행 중인 이슈
- ⚠️ 이미지 업로드 UI: 백엔드 API 준비 완료, 프론트엔드 UI 연동 필요
- ⚠️ 메시지 시스템 UI: 백엔드 API 준비 완료, 프론트엔드 UI 연동 필요
- ⚠️ 검색 및 필터링 API: 백엔드 구현 필요, 프론트엔드 대기 중

### 3. 향후 개선 사항
- [ ] API 응답 캐싱 전략 수립
- [ ] 무한 스크롤 성능 최적화
- [ ] 이미지 최적화 (WebP 변환, Lazy Loading)

---

## 📅 통합 마일스톤

### Phase 1: 기본 통합 완료 (완료)
- ✅ 인증 시스템 통합
- ✅ 마스터 데이터 API 연동
- ✅ 팝업 CRUD API 연동
- ✅ 리뷰 CRUD API 연동
- ✅ 판매자 API 연동

### Phase 2: 고급 기능 통합 (진행 중)
- ✅ Kakao Map API 통합 (완료)
- ✅ 관리자 기능 UI 연동 (완료)
- 🚧 메시지 시스템 UI 연동
- ⏳ 검색 및 필터링 API 연동

### Phase 3: 최적화 및 배포 준비 (예정)
- ⏳ 성능 최적화
- ⏳ 통합 테스트 작성
- ⏳ 프로덕션 배포

---

## 📝 통합 체크리스트

### 백엔드 준비 사항
- [x] API 엔드포인트 구현
- [x] 인증 시스템 구현
- [x] 에러 처리 구현
- [x] API 문서화 (Swagger)
- [ ] 검색 API 구현
- [ ] 추천 API 구현

### 프론트엔드 준비 사항
- [x] API 서비스 레이어 구현
- [x] 인증 Context 구현
- [x] 에러 처리 구현
- [x] 주요 UI 컴포넌트 구현
- [ ] Kakao Map 통합
- [ ] 검색 UI 구현

### 통합 테스트
- [ ] API 통합 테스트 작성
- [ ] E2E 테스트 작성
- [ ] 수동 테스트 체크리스트 완료

### 배포 준비
- [ ] 환경 변수 설정 완료
- [ ] 빌드 스크립트 작성
- [ ] 배포 문서 작성

---

## 🔗 관련 문서

- [백엔드 개발 계획서](BE-plan.md)
- [프론트엔드 개발 계획서](FE-plan.md)
- [프로젝트 메인 README](../../README.md)
- [API 문서](../../docs/api/REST_API_문서.md)
- [배포 가이드](../../docs/deployment/DEPLOY_TO_PRIVATE_EC2.md)

---

**최종 업데이트**: 2025-01-27 (Kakao Map 통합 완료, 권한 설정 완료, 더미 데이터 스크립트 작성 완료)  
**작성자**: AI Assistant  
**검토 상태**: 진행 중

